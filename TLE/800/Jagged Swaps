#include<bits/stdc++.h>
using namespace std;

void solve() {
    int t; cin>>t;
    while(t--) {
        int n; cin>>n;
        int a[n];
        for(int i=0; i<n; ++i) cin>>a[i];
        if(a[0]==1) cout<<"Yes\n";
        else cout<<"No\n";
    }
}

int main() {
    solve();
    return 0;
}
