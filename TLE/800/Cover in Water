#include<bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);
	int t; cin >> t;

	while (t--) {
	    int n; cin >> n;
        string s = ""; cin >> s;
        int c;
        c = (s.find("...") != -1) ? 2 : count(s.begin(),s.end(),'.');
        cout << c << endl;
	}
	
	return 0;
}
